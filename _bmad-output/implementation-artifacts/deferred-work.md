# Deferred Work

## Deferred from: code review of story 7.A.6 (2026-05-14)

- **Absorption 23505 aveugle dans `logNotification`** [`lib/notifications-log.ts:57`] — Tout code 23505 est silent-skip sans vérification du nom de contrainte. Si un futur UNIQUE INDEX est ajouté sur `notifications_log`, son 23505 serait silencieux. Candidat 7.A.11 : enrichir le breadcrumb avec `constraint_name` extrait de `insertError.details` ou `insertError.hint`.
- **Cast `insertError as { code?: string }` sans type guard runtime** [`lib/notifications-log.ts:57`] — Pattern stable (SDK Supabase expose `.code` de manière constante, déjà validé story 3.4) mais non garanti contractuellement. Candidat hardening si le SDK change de format d'erreur.
- **`Sentry.captureException` peut throw dans le catch `logNotification`** [`lib/notifications-log.ts:76`] — Pré-existant avant 7.A.6. Si le SDK Sentry est indisponible, une exception propagera hors du catch vers le caller. Candidat hardening commun avec story 7.A.3 defer (`safeSentryCapture`).

## Deferred from: code review of story 7.A.5 (2026-05-14)

- **`sendMessage` conversation fetch : `error` DB non capturee, alias "non trouvee"** [`app/actions/messages.ts:376-391`] — Pre-existant avant 7.A.5 : le destructuring omet `error` sur la query `.single()`. Une erreur DB transitoire renvoie "Conversation non trouvee" au lieu d'un vrai message d'erreur, sans Sentry capture. Candidat hardening messagerie.
- **`check-oracle-paywall.mjs` : blocs `/* */` multi-lignes non filtres** [`scripts/check-oracle-paywall.mjs`] — Pre-existant dans le script original. Un commentaire JSDoc contenant `contacter [role]` declencherait un faux positif CONTACT_RE. Ajouter un suivi d'etat "dans un bloc /* */" pour filtrer correctement.
- **Race condition refetch null sans diagnostic Sentry distinct (23505 + refetch still null)** [`app/actions/messages.ts:138-157,250-266,335-352`] — Apres un 23505 + refetch qui retourne `data: null, error: null` (row supprimee entre INSERT et refetch), aucun signal Sentry specifique. Ajouter un `safeSentryCapture` avec signal distinct `'refetch-null-after-23505'` pour tracer ce cas limite.
- **Test `message-unifie-anti-oracle.test.ts` n'exerce pas accompagnant comme sender dans `sendMessage`** [`tests/integration/paywall/message-unifie-anti-oracle.test.ts`] — Cas (c) teste uniquement l'accompagne comme sender. Un futur refactor qui exemptait `isAux` du paywall ne serait pas detecte. Ajouter scenario `(e)` : accompagnant abonnement expire + conv existante + sendMessage.

## Deferred from: code review of 7-a-4-aggreger-n-plus-1-unread-counts-admin-messages (2026-05-14)

- F6 — Sous-requête corrélée COUNT sans index composite `(conversation_id, sender_id, read_at)` dans `get_admin_conversations_with_unread` : l'index `idx_messages_conversation` couvre le lookup mais pas le filtre `sender_id + read_at`. Candidat optimisation si volumétrie messages dépasse 10k [supabase/migrations/20260513230753_admin_messages_rpc_unread_aggregation.sql:40]
- F7 — `p_limit=100` hardcodé dans la page admin/messages, pas de pagination UI. Conversation list tronquée silencieusement au-delà de 100 entrées [app/admin/messages/page.tsx:16]
- F12 — Race condition refetch dans `getOrCreateConversation` : si la row est supprimée entre le moment de l'insert 23505 et le refetch, `refetched` est null et une erreur est retournée à l'utilisateur. Comportement documenté par commentaire, candidat hardening si la suppression de conversations devient possible côté admin [app/actions/messages.ts:137-147]
- F14 — `p_limit` et `p_offset` négatifs acceptés par la RPC (LIMIT -1 = pas de limite). Surface d'attaque nulle car seul admin peut appeler et la valeur est hardcodée, mais validation défensive optionnelle pour robustesse future [supabase/migrations/20260513230753_admin_messages_rpc_unread_aggregation.sql:8-9]

## Deferred from: code review of 7-a-3-garde-fou-skip-e2e-tests-interdit-en-prod (2026-05-14)

- `safeSentryCapture` catch vide sans fallback logging dans `messages.ts` : si le SDK Sentry est mal initialisé, les erreurs critiques de messaging disparaissent sans trace. Design documenté, trade-off accepté pour server actions return-path ; candidat hardening Sentry dédié [app/actions/messages.ts:29-33]
- `Sentry.flush` absent du mock `@sentry/nextjs` dans le setup de test : la garantie « flush avant throw » dans `subscription-helpers.ts` n'est pas testée ; le test vérifie `captureException` mais pas `flush`. Le comportement en prod est correct, mais le test ne verrouille pas l'invariant [tests/integration/setup.ts]

## Deferred from: code review of 7-a-1-robustifier-hasactivesubscription-et-getorcreateconversation (2026-05-13)

- Cron `confirm-parrainages` : le throw de `hasActiveSubscription` est rattrapé par le catch de boucle existant mais incrémente `errors` sans distinguer erreur-métier vs incident-BDD transitoire [app/api/cron/confirm-parrainages/route.ts:48] — amélioration de sémantique hors scope 7.A.1 ; candidat 7.A ou cron hardening dédié
- Race condition `accompagnes_profiles` : le pattern "lookup → insert si absent" n'est pas protégé par ON CONFLICT ni UNIQUE sur `user_id` ; deux appels concurrents peuvent créer un double profil [app/actions/messages.ts:67-82] — pré-existant avant 7.A.1 ; candidat hardening profils
- `getSubscriptionStatus` encore en `.single()` sans guard error — divergence avec `hasActiveSubscription` durci en 7.A.1 ; erreur Supabase transitoire silencieuse dans les pages settings [lib/subscription-helpers.ts:80] — hors scope story (Dev Notes l'exclut) ; candidat 7.A.x ou mini-epic hardening helpers

## Deferred from: code review of 2-1-validation-automatique-filleule (2026-04-28)

- Quota marraine illimité (parrainages × N infini) [app/actions/parrainage.ts] — reporté Story 2.2 (compteur récompense + plafond éventuel)
- Pas de contrainte CHECK sur format `code` en base [migration:9-16] — robustesse uniquement, pas de bug actuel car `normalizeCode` est single-source
- Asymétrie FK : `parrainages.marraine_id ON DELETE CASCADE` vs `parrainages.filleule_id ON DELETE SET NULL` — si la marraine supprime son compte entre l'inscription et le paiement de la filleule, la filleule reste en limbo (`parrainee_par` non null mais relation parrainage disparue) → à documenter Story 2.3
- `admin_actions_log.admin_id` accepte NULL sans contrainte CHECK qui restreint cela à `action_type = 'validation_par_parrainage'` — accepté pour MVP, à doc opérationnelle
- Pas de fallback webhook si la filleule paie mais ne revient jamais sur `/accompagnante/abonnement/success` (onglet fermé, retour navigateur, paiement asynchrone SEPA) — reporté Story 2.2 (webhook dédié), mais le risque opérationnel concret doit être mentionné dans `TODO-LAUNCH.md` pour la phase de tests preview
- Header `x-forwarded-for` non sanitizé : potentiel spoof si l'app n'est pas systématiquement derrière un reverse proxy de confiance [app/actions/auth.ts:88-96] — Vercel sanitize en prod, à doc Story 2.3 anti-fraude
- Step parrainage du `RegisterForm` non visité si l'utilisateur arrive avec un `email` déjà pré-rempli via URL [components/auth/register-form.tsx:73-77] — UX rare, fix sans gain immédiat
- Race au signup rapide : deux inscriptions simultanées avec le même code valide (pas de UNIQUE `(filleule_id)` sur `parrainages`) — Story 2.3 blacklist couvrira partiellement
- `users.single()` dans `onboarding/page.tsx` peut théoriquement crash si la création de la ligne `users` par `handle_new_user` n'a pas encore eu lieu pour un user fraîchement créé — `handle_new_user` est synchrone Postgres, cas non reproductible mais à surveiller

## Deferred from: code review of 2-2-ui-parrainage-card-et-fingerprint-stripe (2026-04-28)

- Marraine `valide` sans row `parrainages_codes` (échec retry x3 ou backfill rétroactif manquant) [app/accompagnante/dashboard/page.tsx:37-45] — la card disparaît silencieusement, pas de fallback "Générer mon code". À traiter dans le ticket de backfill rétroactif déjà listé en Dette technique de la Story 2.2.
- `navigator.clipboard.writeText` rejet silencieux sans fallback visible [components/accompagnante/parrainage-card.tsx:16-22] — accepté par Dev Notes (« UX gracefully dégradée acceptable »). À reconsidérer si retours utilisateurs.

## Deferred from: implementation of 2-3-blacklist-admin-anti-fraude (2026-04-28)

- Détection `meme_telephone` non implémentée : `users.phone_hash` n'est jamais peuplé par le code applicatif et `users.phone` n'est pas demandé au signup (formulaire 4 champs). À traiter quand le téléphone deviendra obligatoire au signup ou via un cron quotidien qui re-évalue les parrainages ouverts en croisant `users.phone`.
- Suspension automatique de la marraine en cas de fraude confirmée non implémentée : décision MVP pour éviter les faux positifs (la filleule peut avoir tenté une fraude unilatérale). L'admin tranche manuellement via la page utilisateurs.
- Normalisation avancée d'adresse (gestion accents, géocodage, abréviations rue/avenue/bd) : V2 anti-fraude. MVP : comparaison littérale + flag pour revue admin suffisant.
- Masque /24 ou géolocation IP non implémenté : MVP : comparaison littérale `ip_inscription`. Cas IP partagée (NAT, opérateur mobile) = faux positif acceptable côté flag (l'admin tranche).
- Cron de re-évaluation périodique des parrainages ouverts (rattrapage signaux différés type changement de carte ou modif adresse profil) non implémenté : webhook capture déjà les signaux post-paiement. Cas marginaux non couverts.
- Backfill historique : la détection ne s'applique pas rétroactivement aux parrainages existants pré-Story-2.3. Probablement aucun parrainage en prod à date (table 0 rows).
- Pas de UNIQUE constraint sur `parrainages(filleule_id)` ou `(marraine_id, filleule_id)` : la détection blacklist limite déjà le risque pratique d'une race au signup rapide.

## Deferred from: code review of 2-3-blacklist-admin-anti-fraude (2026-04-28)

- Migration `idx_users_email_lower` / `idx_parrainages_blacklist` sans `CONCURRENTLY` [supabase/migrations/20260428144601_parrainage_blacklist_constraints.sql] — lock écriture sur `users` pendant la création. Volume actuel ~320 users négligeable, à refactorer si la table grossit (>10k rows).
- `normalizeAddress` sans gestion accents [lib/parrainage-detection.ts] — décision MVP documentée Dev Notes (`8 rue Albéric` ≠ `8 rue Alberic`). Faux négatifs assumés.
- `normalizeEmail` sans gestion `+suffix` Gmail / IDN punycode [lib/parrainage-detection.ts] — décision MVP documentée Dev Notes. Faux négatifs assumés.
- Détection IP sans normalisation IPv6/IPv4-mapped (`::ffff:1.2.3.4` vs `1.2.3.4`) [app/actions/parrainage.ts:60-72] — signal flag uniquement, faux négatifs acceptables.
- Stripe `paymentMethods.list` sans pagination (limit 10) [app/api/webhooks/stripe/route.ts:97] — faux négatif si la marraine a >10 cartes attachées (cas marginal).
- Pas de pagination sur `/admin/parrainages/blacklist` [app/admin/parrainages/blacklist/page.tsx] — charge tous les rows blacklistés en mémoire. OK MVP volume faible, à risque si volume grossit.
- `notes` admin sans cap longueur [app/actions/admin-parrainages.ts] — `details: { notes }` jsonb peut grossir. Cap à 5000 chars à ajouter si abus.
- Email admin fallback silencieux sur `RESEND_FROM_EMAIL` [lib/emails.ts:2023-2030] — si `ADMIN_NOTIFICATIONS_EMAIL` non configurée, email part vers l'adresse `from` (no-reply). À documenter ou fail loud plus tard.

## Deferred from: code review of 2-3-blacklist-admin-anti-fraude (2026-05-04)

- `validateCode` exposé sans rate-limit (oracle d'énumération de codes 8 chars) [app/actions/parrainage.ts:255-298] — reconnu dans la spec comme « à traiter séparément ». Combiner avec rate-limit IP global et/ou refondre en route API protégée.
- `console.error` sans alerting/Sentry partout dans les chemins fraude/email/blacklist — dette pré-existante de toute la stack, pas spécifique à 2-3. À traiter de façon transverse (Sentry + alertes côté monitoring).
- Types `as any` dans pages admin [app/admin/parrainages/page.tsx:185, app/admin/parrainages/blacklist/page.tsx:134] — dette TS pré-existante. À typer proprement quand la BDD aura des types Supabase générés stables.
- `escapeHtml` dans `lib/emails.ts` non visible dans le diff — dépendance externe utilisée intensivement, à valider hors review (test XSS dédié sur les templates email).

## Deferred from: code review of 2-5-1-outillage-a11y-baseline-lint (2026-05-04)

- `npm run lint` retourne exit 0 avec 231 warnings (160 a11y + 71 tseslint) -- faux signal "tout clean" en local. Decision tech-spec assumee (mode warn au demarrage), bascule en `error` planifiee post-Lot A complet (toutes les stories 2.5.x livrees).
- `downgradeErrorsToWarn` masque toutes les erreurs ESLint critiques au-dela d'a11y (`no-undef`, `no-unused-vars`, `@typescript-eslint/no-explicit-any`...) -- intentionnel pour bootstrap, a reverser post-Lot A complet.
- `lint:fix` peut modifier `scripts/build-a11y-baseline.mjs` lui-meme (autofix `} catch {}` -> `/* empty */`) -- pollution diff potentielle. Fix : ajouter `scripts/` aux ignores du flat config ou corriger la directive `} catch {}` source.
- `findLatestBaseline()` trie par nom (date ISO) -- un baseline genere avec clock skew futur (CI runner desynchronise) sera selectionne indefiniment, masquant des regressions massives jusqu'a la date reelle. Ajouter une validation `date <= today` dans le wrapper.
- Position de la section "## DoD a11y" dans le template `bmad-create-story` -- placee apres `## Dev Agent Record / ### File List`, descend sous le fold sur stories volumineuses. Risque de DoD oubliee. Deplacer plus haut si necessaire.

## Deferred from: code review of 2-5-4-prefers-reduced-motion (2026-05-05)

- Pas de cleanup des `setTimeout` cascade au unmount [components/landing/hero-carte.tsx:179-185] -- pre-existant avant cette story (la cascade existait deja). Le `setTimeout` outer 1800ms et les inners 150ms*N ne sont pas clear au demontage. Strict Mode peut produire des indices doublons dans `visibleCities`. A traiter dans une story future de hardening hero-carte.
- Toggle reduce -> normal asymetrique sur le path stroke [components/landing/hero-carte.tsx:165-173] -- tolerance acceptable per spec (AC8). Si l'utilisateur arrive avec `reduce` actif puis le desactive, le contour Bretagne reste statique, pas de re-trace. Documente.

## Deferred from: code review of 2-5-5-composant-input-accessible (2026-05-05)

- Violations baseline `label-has-for (1) + label-has-associated-control (1)` sur `register-form.tsx` [components/auth/register-form.tsx:233] -- pre-existantes, viennent d'un autre label orphelin (step `Role` qui groupe des boutons radio non-input « Vous etes »), pas du label `parrainage_code`. Decouvert post-application D2. La spec story Dev Notes ligne 235 attribuait incorrectement les 3 violations au label `parrainage_code`. Reel split : 1 du `parrainage_code` (resorbe par D2) + 2 du step `Role` (ce defer). A traiter dans une story dediee Lot B (refactor des labels groupant des controles non-input via fieldset/legend ou `role="radiogroup"` + `aria-labelledby`).
- Violations baseline `jsx-a11y/no-autofocus x4` sur `register-form.tsx` + `x1` sur `app/login/page.tsx` -- hors scope explicite story 2.5.5 (Dev Notes ligne 235). A traiter Lot B ou story dediee.
- Test manuel VoiceOver `/forgot-password` ET `/register?role=accompagnante` (Task 3 cochee `[ ]`) -- bloqueur pour passage en `done`, hors revue de code statique. A executer par Sylvain avant merge.

## Deferred from: code review of 2-6-1-outillage-axe-core-playwright (2026-05-05)

- Nouveau parcours avec 0 violation passe silencieusement jusqu'a regen manuelle [scripts/check-axe-baseline.mjs:126-135] -- comportement acceptable, le warning explicite suffit, regen baseline = process documente dans tests/a11y/README.md.
- Pas de garde explicite si port 3000 deja occupe en CI [playwright.config.ts:27-28] -- message Playwright standard suffit, audit local pour l'instant. A revoir avec la bascule CI Lot C.
- Webserver timeout 120s peut etre court en CI cold cache [playwright.config.ts:27] -- pas de CI Vercel actuellement pour la suite Playwright, a revoir avec la bascule CI Lot C.
- `PLAYWRIGHT_JSON_OUTPUT_NAME` et `reportFile` hardcodes en sync dans build/check-axe-baseline.mjs [scripts/build-axe-baseline.mjs:44-55] -- pas un bug actuel, latent si refacto. A documenter en commentaire si modification.

## Deferred from: code review of 2-7-1-refactor-main-pages-client (2026-05-06)

- `kraft bg-kraft` redondant repete sur les 2 wrappers `<main>` (`app/register/page.tsx`, `app/accompagnante/onboarding/page.tsx`) -- pre-existant, present avant le refactor sur les composants. Si `kraft` definit deja `background-color`, `bg-kraft` est doublon ou override silencieux. Hors scope story 2.7.1 (refactor structurel `<main>`). A clarifier dans une story de hardening Tailwind.
- `<Suspense>` sans `fallback` dans `app/register/page.tsx` -- pre-existant, hors scope refactor structurel `<main>`. Surface d'ecran blanc theorique pendant l'hydratation cote client (si SSR ne pre-rend pas le formulaire). A traiter si UX percue.
- Skip-link `/accompagnante/onboarding` non couvert par axe-check automatise -- documente dans la spec story 2.7.1 AC7 Note explicite : page hors 7 parcours critiques. Couverture statique uniquement (lint a11y + DOM grep). Decision Lot C de ne pas etendre la suite axe-core a cette page.
- Parite `animate-fade-in` au mount non validee visuellement post-refactor -- risque theorique mineur, React diffing preserve normalement l'identite des nodes (pas de remount lors du changement de wrapper externe `<main>` -> `<div>`). Couvert par DoD a11y manuelle (Sub 2.3, 2.4, 3.3 marquees a executer par l'utilisateur).
- Pas de `.editorconfig` ni `.prettierrc` dans le projet -- la reindentation -2 espaces sur ~200 lignes (`register-form.tsx`) ne sera pas validee par un formatter automatique. Risque d'incoherence si un dev futur lance Prettier avec des defauts differents. A traiter en story d'outillage dev-experience.

## Deferred from: code review of 2-7-6-refactor-main-pages-auth-jumelles (2026-05-06)

Tous ces findings concernent du code **strictement preserve** depuis HEAD (verifie via `git show HEAD:app/{login,forgot-password,reset-password}/page.tsx`). Aucune regression introduite par 2.7.6 — ce sont des dettes anterieures relevees par les reviews adversariales sur le code migre tel quel.

- Enumeration de comptes via `checkEmailExists` (login-form.tsx:25-29) -- le flux distingue publiquement compte existant (step password) vs absent (redirect `/register?email=...`). Contradiction avec la convention `/forgot-password` qui masque l'existence. Securite auth, story dediee.
- `autoFocus` sur input password (login-form.tsx:104) -- pratique a11y discutable (focus deplace sans annonce AT). Accepte en baseline jsx-a11y/no-autofocus, hors scope refactor structurel.
- `formData.set('email', email)` overwrite cote client sans hidden input ni revalidation serveur visible (login-form.tsx:41) -- audit securite a faire dans une story dediee Server Actions.
- Race condition : submit pendant qu'un submit precedent est en cours (3 forms) -- pas de garde `if (loading) return` en debut de handler. Double appel possible si user clique vite ou Enter+clic.
- Server Action throw vs `{error}` non gere (3 forms) -- `await login/resetPassword/updatePassword` sans `try/catch`. Si l'action throw (network, timeout), `loading` reste true, UI gelee.
- `setLoading(false)` manquant apres succes `login()` (login-form.tsx:43-46) -- le code repose sur la redirection serveur pour unmount. Si la redirection echoue silencieusement, bouton reste desactive sans recours.
- setState apres unmount sur navigation pendant submit (3 forms) -- pas de `mountedRef`. Warning React possible, fuite memoire mineure.
- `role="alert"` rendu conditionnel non annonce par certains AT (3 forms) -- le role doit etre present au montage pour annoncer le contenu inseré apres. Conteneur live persistant manquant.
- Region succes non `aria-live`/`role="status"` (forgot-password-form.tsx:36-48, reset-password-form.tsx:36-48) -- les blocs "Email envoye" / "Mot de passe mis a jour" remplacent le formulaire sans annonce auditive.
- Hierarchie de titres cassee : `<p className="font-semibold">` au lieu de `<h2>` pour "Email envoye", "Mot de passe mis a jour", "Reinitialiser votre mot de passe" (forgot/reset). Pas de structure de titres exploitable au lecteur d'ecran.
- Lien "Retour a la connexion" duplique sur forgot-password (forgot-password-form.tsx, 2 occurrences en cas de succes) -- anti-pattern a11y (2 liens identiques meme destination).
- Token reset non verifie cote page (reset-password) -- la page affiche le formulaire sans verifier la presence d'une session reset valide via Supabase. Acces direct URL produit erreur generique au submit. Securite/UX, story dediee.
- Validation client `email.trim()` insuffisante (login-form.tsx:20) -- accepte `"a"`. Pas de regex format avant round-trip serveur. `e.preventDefault()` court-circuite la validation HTML5 native.
- `relative z-10` dead style sur les 3 wrappers `<div className="w-full max-w-md relative z-10">` -- aucun element en `absolute` derriere dans la nouvelle structure. Heritage copier-coller du pre-refactor.
- Refactor manque : 3 composants dupliquent `<div className="text-center mb-8"><Link href="/">roxanetnous</Link>...</div>` + carte blanche + alerte erreur. Opportunite `AuthCard` partage hors scope 2.7.6 (centre sur l'extraction Server/Client, pas sur la factorisation visuelle).

## Deferred from: code review of 2-7-2-heading-order-cards-h3-h2 (2026-05-06)

- Saut h2 -> h4 sur `app/admin/page.tsx` (h3 ligne 97 promu en h2, mais 3 h4 lignes 189/270/280 « Repartition », « MRR par segment », « Resiliations » laisses intacts) -- pre-existant. Documente explicitement dans Decisions techniques de la story (ligne 248) hors scope 2.7.2. Story Lot D pressentie pour traiter le saut a4 -> h3 dans DashboardTabs.
- `<h2>` "Nom Prenom" dans la carte profil avatar des dashboards (`app/accompagnante/dashboard/page.tsx:109` et `app/accompagne/dashboard/page.tsx:75`) coexiste comme sibling des nouvelles `<h2>` "Messages"/"Mon profil"/"Mes annonces". Le nom utilisateur lit alors comme une section de page, ce qui est semantiquement bizarre pour la navigation par titres screen-reader. Choix conscient documente (Decisions techniques ligne 246) : « le h2 Nom Prenom est semantiquement un titre de carte au meme niveau que les autres cards ». A reconsiderer si retours utilisateurs AT.
- Accord grammatical "Tous les accompagnantes" -> "Toutes les accompagnantes" [`app/recherche/page.tsx:382`] -- copy bug pre-existant (etait deja "Tous les accompagnantes" en h3 avant le refactor). Hors scope 2.7.2, a corriger dans une story de polish copy.
- Copy "Demandes accompagnés" ungrammatical [`app/accompagnante/dashboard/page.tsx:318`] -- attendu « Demandes des accompagnes » ou « Demandes daccompagnes ». Pre-existant, hors scope 2.7.2.

## Deferred from: code review of 2-7-3-page-publique-accessibilite (2026-05-06)

- Pas d'`aria-current="page"` sur les liens du nav `<nav aria-label="Liens legaux">` (4 liens dans `components/footer.tsx:13-35`) -- pour AT naviguant via le footer, le lien correspondant a la page courante n'est pas annonce comme tel. Defaut partage par les 4 liens (mentions-legales, politique-de-confidentialite, accessibilite, cgu). Story future Lot D pressentie.
- `focus:outline-none` sur le `<main id="main-content" tabIndex={-1}>` -- WCAG 2.4.7 (Focus Visible) : le skip-link `#main-content` (defini `app/layout.tsx:62-66`) declenche le focus programmatique sur `<main>`, mais aucun anneau de focus visible. Pattern partage par TOUTES les pages publiques (`app/cgu/page.tsx:7`, `app/mentions-legales/page.tsx:7`, `app/politique-de-confidentialite/page.tsx:7`, `app/accessibilite/page.tsx:13`). Heritage Lot A 2.5.2. A traiter en story dediee si ajout d'un focus-ring intentionnel sur `<main>` valide visuellement.
- Contraste `text-gray-700` sur `bg-kraft` non valide par axe-core (page hors 7 parcours) -- calcul theorique ~6.0:1 (AA OK) mais l'overlay `kraft::before` (`globals.css:75-91`, opacity 0.35 + mix-blend-mode multiply) altere la mesure effective. Pas de CI sur cette page. Lie a la decision sur la couverture des parcours axe (decision-needed #3 de la review).
- Email contact `outlook.com` vs `roxanetnous.fr` -- divergence projet : `roxanetnous@outlook.com` est utilise dans toutes les pages publiques (`mentions-legales:22`, `politique-de-confidentialite:19,75,79`, `accessibilite:79,82`, `app/actions/contact.ts:8`, `lib/emails.ts:886`, `components/abonnement/subscription-page-content.tsx:6`) MAIS `contact@roxanetnous.fr` apparait dans `README.md:265`, `_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-18-parrainage.md:19,878`, `architecture-technique-roxanetnous-2026-02-09.md:4162`, `sprint-change-proposal-2026-04-18-visio-validation.md:96,334`. Probleme global hors story 2.7.3. A trancher : email canonique unique + harmonisation.
- Lien « Retour a l'accueil » sans `focus-visible:` style explicite (`app/accessibilite/page.tsx:14`, mais aussi `cgu/page.tsx:8`, `mentions-legales/page.tsx:8`, `politique-de-confidentialite/page.tsx:8`) -- repose sur le focus UA par defaut, qui peut etre supprime ailleurs par un global outline:none. Pattern partage par les 4 pages publiques. Lie au focus-token Lot A 2.5.3 mais le token semble ne pas s'appliquer sur ce pattern de lien.
- Aucune couverture E2E/axe-core sur `/accessibilite` (page hors des 7 parcours du baseline axe-core) -- decision Project Lead 2026-05-06 : dette acceptee. Page statique sans JS, sans formulaire, sans etat dynamique : risque de regression silencieuse tres faible. Ajout d'un 8eme parcours Playwright non rentable pour 100 lignes de markup fige. A reconsiderer si la page evolue avec composants client (ex. formulaire de signalement direct, widget feedback, etc.).

## Deferred from: code review of 2-7-3-page-publique-accessibilite (passe 2 - 2026-05-06)

- Espaces insecables dans listes peuvent forcer overflow horizontal a zoom 200% sur mobile <320px [`app/accessibilite/page.tsx:61`] -- `«&nbsp;ville&nbsp;»` et `«&nbsp;rayon&nbsp;»` ne peuvent pas se couper, le wrapper `max-w-3xl` n'a pas d'overflow-wrap explicite. Edge case zoom 200% pas dans la DoD a11y projet (la page elle-meme reconnait que la cible tactile 44x44 px n'est pas auditee systematiquement). A reconsiderer si la DoD a11y inclut explicitement zoom 200% au Lot D.
- `<code>` contraste/zoom non verifie sur `bg-kraft` [`app/accessibilite/page.tsx:36-42, 59`] -- 4 occurrences `<code>eslint-plugin-jsx-a11y</code>`, `<code>axe-core</code>`, `<code>aria-hidden</code>`, `<code>inert</code>`. Le style UA `<code>` peut ajouter un fond legerement different, et hereditent de `text-gray-700` deja contestable sur `bg-kraft` (defer pre-existant passe 1). Page hors des 7 parcours axe, donc pas de gate. A verifier visuellement zoom 200% si une story de polish a11y est ouverte.
- Pas de `<time datetime="2026-05-06">` autour des dates [`app/accessibilite/page.tsx:25,28,92`] -- bonnes pratiques semantiques recommandent `<time>` pour permettre aux assistants/extracteurs de lire les dates. Pattern projet : les pages jumelles `cgu/page.tsx:128`, `mentions-legales/page.tsx`, `politique-de-confidentialite/page.tsx` n'utilisent pas non plus `<time>`. A traiter en story de polish semantique cross-page si jamais lancee.
- Lien « Retour a l'accueil » texte identique entre 4 pages publiques [`app/accessibilite/page.tsx:14-16`, `cgu/page.tsx:8-10`, `mentions-legales/page.tsx:8-10`, `politique-de-confidentialite/page.tsx:8-10`] -- en navigation par liste de liens VoiceOver (`Ctrl+Opt+U` puis `L`), 4 entrees identiques peuvent etre ambigues si plusieurs pages sont ouvertes en onglets. Pattern partage entre les 4 pages, hors scope story 2.7.3.
- Pas de lien interne vers pages jumelles depuis le contenu [`app/accessibilite/page.tsx`] -- asymetrie avec `cgu/page.tsx:91-94` qui linke vers `/politique-de-confidentialite` dans la section Donnees. Faible impact (le footer est present sur toutes les pages, donc l'utilisateur peut naviguer). A reconsiderer si la page evolue.
- Email `outlook.com` adresse personnelle pour obligation legale [`app/accessibilite/page.tsx:80`] -- redondant avec le defer pre-existant passe 1 (deja entree dans cette section). Conserve pour traçabilite passe 2.
- `text-gray-700` sur `bg-kraft` contraste limite [`app/accessibilite/page.tsx:20`] -- redondant avec le defer pre-existant passe 1. Conserve pour traçabilite passe 2.

## Deferred from: code review of 2-7-3-page-publique-accessibilite (passe 3 - 2026-05-06)

- Date dupliquee « 6 mai 2026 » a 2 endroits sans constante partagee [app/accessibilite/page.tsx:32-35] -- ligne « Cette declaration a ete etablie le 6 mai 2026 » + ligne « Derniere mise a jour : 6 mai 2026 ». Risque de desync a la prochaine maj editoriale (oubli de l une des deux). Pattern accepte passe 1 decision 2b (etablissement + maj distinctes), pas un bug, dette de maintenance editoriale. A factoriser via constante si la page evolue.
- Sitemap `lastModified: new Date()` recalcule a chaque fetch [app/sitemap.ts:17] -- pre-existant sur les 7 entrees du sitemap, non introduit par 2.7.3. Defeats `changeFrequency: yearly` cote crawler (lastmod avance a chaque crawl). A traiter de facon transverse sur toutes les pages statiques (story sitemap dediee), pas un blocker 2.7.3.
- Tokens techniques anglais sans `lang="en"` switch [app/accessibilite/page.tsx:43-50] (`axe-core`, `Playwright`, `VoiceOver`, `eslint-plugin-jsx-a11y`) -- TTS francais (VoiceOver fr, NVDA fr) peut mal prononcer les proper nouns anglais. Hors DoD projet (tests TTS narratifs Lot B couvrent messagerie/onboarding/formulaires, pas la page de declaration). A wrapper en `<span lang="en">` si une revue manuelle TTS sur `/accessibilite` est demandee plus tard.

## Deferred from: code review of 2-7-3-page-publique-accessibilite (passe 3 decision tranchee - 2026-05-06)

- **Footer overflow viewport <320px (4 liens en `flex gap-6` sans `flex-wrap`)** [components/footer.tsx:11] -- decision Project Lead 2026-05-06 : option 3 defer. Le `<nav aria-label="Liens legaux">` passe a 4 liens (Mentions legales 14ch, Politique de confidentialite 26ch, Accessibilite 13ch, CGU 3ch). Sur mobile <320px, le `flex gap-6` deborde horizontalement (overflow ou clipping). Justification : cible 320px hors DoD a11y projet (zoom 200% non audite, cf. AC3 limites connues page accessibilite), siblings footer identiques (la modification est la 4eme entree, pas un defaut introduit par 2.7.3 specifique), aucun parcours axe-core ne couvre cette largeur, risque cosmetique uniquement. A traiter dans une story polish footer transverse si la couverture mobile <320px devient une exigence projet (zoom 200% systematique, audit cible tactile, etc.).

## Deferred from: code review of 2-7-4-bascule-axe-check-bloquant (2026-05-06)

- Synchronisation `tests/a11y/README.md` : passer « 6 parcours critiques » a « 7 parcours audites (P1-P6 dont P4 = 2 sous-entrees `p4-login` + `p4-register`) » dans intro ligne 3 et titre tableau ligne 24 -- divergence avec `.claude/CLAUDE.md` ligne 6 (modifie par Story 2.7.4 : passe a « 7 parcours »). Risque R4 anticipe par la Story 2.7.4 elle-meme. Note technique : 6 spec files Playwright MAIS 7 entrees dans le baseline JSON (P4 = 2 sous-entrees). Le README ligne 31 documente deja les 2 sous-entrees ; seules l'intro et le titre tableau utilisent le compte « 6 ». A traiter en story 2.X.Y de polish doc apres decision retro Lot C 2.7.5.

## Deferred from: code review of 2-7-4-bascule-axe-check-bloquant (passe 2 - 2026-05-06)

- **Elargissement R4 : 5 fichiers projet contredisent la nouvelle regle CLAUDE.md « 7 parcours »** -- l auto-review passe 1 R4 ne mentionnait que `tests/a11y/README.md`. La verite est plus large : `_bmad-output/planning-artifacts/prd.md:17,272,283,291` (autorite produit), `_bmad-output/test-artifacts/nfr-assessment-a11y-2026-05-04.md:147,184,254,291,321,332,340,375,383,390` (assessment), `_bmad-output/planning-artifacts/inventaire-points-usage-lot-c-2026-05-06.md:99` (inventaire Lot C), `tests/a11y/README.md:3,115` utilisent encore « 6 parcours critiques ». Note technique : 6 = parcours metier definis dans le PRD (P1 onboarding aux, P2 recherche, P3 messagerie, P4 inscription/checkout, P5 landing, P6 suppression RGPD) ; 7 = entrees baseline JSON axe-core (P4 a 2 sous-entrees `p4-login` + `p4-register`). Les deux comptes sont defendables selon la definition. A traiter en story 2.X.Y de polish doc apres decision retro Lot C 2.7.5 : (a) decider du compte canonique (6 metier OU 7 baseline) ; (b) ajouter une note de reconciliation explicite dans le PRD ou un glossaire ; (c) synchroniser tous les documents en consequence.
- Format `Story X.Y.Z : ...` ambigu pour suffixes lettres [.claude/CLAUDE.md:6] -- historique git contient `Story 2.6.7-A`, `Story 2.6.7-B`, `Story 2.6.7-C` qui ne respectent pas strictement le pattern. Le tech-spec `tech-spec-lot-c-a11y.md:239` cite meme `Story 2.7.X : ...` (X majuscule) comme variante. Aucun document ne formalise la grammaire admise. Pas un blocker (sens commun couvre), mais a clarifier dans la story 2.X.Y de polish doc ci-dessus.

## Deferred from: code review of 2-7-5-retrospective-lot-b (passe 2 - 2026-05-06)

- **« 24 occurrences » Story 2.6.4 ambigu blocs vs fichiers** [`mini-epic-2-6-retro-2026-05-06.md:16`] -- la retro dit « 24 occurrences » comme le bilan source `lot-b-bilan-axe-core-2026-05-06.md:65`. Le commit `aa06f7b` precise « 24 blocs sur 19 fichiers », et le tech-spec `tech-spec-lot-b-a11y.md:87` planifiait 23 fichiers avec un AC `wc -l >= 23`. La couverture effective est 19 fichiers vs 23 planifies. Hérité du bilan source, pas un defaut introduit par la retro. A clarifier dans une story polish doc cross-corpus si la coherence quantitative est souhaitee.
- **Ambiguite « 10 stories splitees en 9 » dans la retro** [`mini-epic-2-6-retro-2026-05-06.md:157`] -- le tech-spec source `tech-spec-lot-b-a11y.md` oscille entre « 8 stories » (lignes 73, 408 - vue haute ou 2.6.7=1) et « 10 stories » (lignes 94, 376 - vue detaillee ou 2.6.7-A/B/C=3). 2.6.1 a 2.6.6 (6 stories) + 2.6.7-A/B/C (3 stories) = 9 stories livrees. La narration « 10 → 9 » de la retro reproduit la confusion source. Pas un bug retro, a clarifier dans le tech-spec source.

## Deferred from: code review of 3-3-affichage-landing-disponible-en-bretagne-depuis-bdd (2026-05-06)

- **Reveal masque le contenu en SSR / no-JS** [`components/landing/reveal.tsx:51-58`] -- le composant `Reveal` (Lot A) applique `opacity-0` au render initial et bascule vers `opacity-100` uniquement apres hydratation + IntersectionObserver. Sans JS, contenu invisible. Non specifique a 3.3, pattern partage par toute la landing depuis Lot A. Candidat story hardening Epic 4 (rendre Reveal `opacity-100` par defaut + animation reverse au mount cote client uniquement) si l'objectif SEO/accessibility no-JS devient explicite.
- **Pas de tests unitaires sur `buildPresentation`** [`components/landing/departements-ouverts.tsx:13-39`] -- fonction pure trivialement testable (4 branches : empty, mono pure, fused-mono Bretagne+44, multi). La regle D1 (Bretagne+44 fused) est subtile et susceptible de regresser sur extension (49 ouvert -> bascule auto multi). Cohérent avec la dette test globale Epic 3 reportee Epic 4 (cf. story 3.3 AC14 « test e2e Playwright reporte Epic 4 »). A traiter quand la batterie unit landing sera mise en place.
- **Validation BDD `region <> ''` / `nom <> ''`** [`supabase/migrations/20260502120000_departements_ouverts.sql`] -- les colonnes `region` et `nom` sont `NOT NULL` mais sans CHECK `<> ''`. Une UPDATE admin avec chaine vide produirait `<h2>Disponible en </h2>` ou `<h3></h3>` (axe-core `empty-heading` violation Critical/Serious). Hors scope story 3.3 (la story consomme la BDD existante), candidat hardening Epic 4 si la confiance dans le formulaire admin est jugee insuffisante.

## Deferred from: code review of 3-4-waitlist-hors-zone-beneficiaire-table-formulaire-et-email (2026-05-06)

- **IP spoofing trivial via `x-forwarded-for`** [`app/actions/waitlist.ts:42-45`] -- pattern projet dette (utilise dans `parrainage.ts:320`). A traiter Epic 4 transverse : utiliser `x-vercel-forwarded-for` (signed Vercel) ou `request.ip`. Affecte tous les rate-limits actuels.
- **Rate-limit fail-open silencieux** [`app/actions/waitlist.ts:54-65`] -- decision explicite (Completion Notes 3.4 + pattern `parrainage.ts:336-341`). Degradation gracieuse acceptee.
- **Mailbombing : pas de double opt-in** [`app/actions/waitlist.ts:89-93`] -- conforme decision D5 (« Pas de captcha ») + R1 (« si abus avere, hCaptcha story dediee Epic 4 »). Risque accepte MVP.
- **Stockage IP brut sans TTL ni purge -- RGPD** [`supabase/migrations/20260506120000_waitlist_departements.sql:11`] -- transverse `parrainages.ip_inscription`, `notifications_log.email`, etc. Story dediee Epic 4 (data retention policy + cron purge >2 ans cf. CNIL).
- **`logNotification` schema drift transverse pour autres flows email** [`lib/emails.ts:24-32`] -- `notifications_log.user_id NOT NULL` + `status CHECK ('pending','sent','failed')` ne supporte pas `user_id null` ni `status='error'`. Bug latent dans `sendContactMessage`, `sendAdminParrainageFlag`, `sendParrainageVerificationEmail` (50 logs en BDD, 0 avec `user_id IS NULL` ou `status='error'` -> ces flows echouent silencieusement). F1 traite uniquement le path waitlist, le refactor transverse est candidat Epic 4 : (a) elargir CHECK status pour accepter `'error'` OU normaliser le code applicatif vers `'failed'` ; (b) ALTER COLUMN `user_id` DROP NOT NULL (necessaire pour tous les flows visiteur anonyme).
- **CASCADE delete waitlist quand admin supprime un dpt** [`supabase/migrations/20260506120000_waitlist_departements.sql:9`] -- decision D1 explicite (CASCADE volontaire, R5/R6 documente). Risque operationnel : si un admin reset `departements_ouverts`, toute la waitlist est purgee sans backup. A reconsiderer si volume waitlist devient significatif (tag `notified_at IS NULL` pour preserver les non-notifies via `ON DELETE RESTRICT`).
- **`BASE_URL` fallback localhost en preview Vercel** [`lib/emails.ts:9, 880`] -- pattern projet, hors scope (transverse infra). Verifier que `NEXT_PUBLIC_BASE_URL` est defini en preview Vercel et prod.
- **`departements` charge deux fois (page + Server Action)** [`app/waitlist/page.tsx:14`, `app/actions/waitlist.ts:35`] -- faux probleme : `getAllDepartements` utilise `unstable_cache` 30s donc 2e lecture = cache hit. Garde traçabilite review.
- **`escapeHtml` non audite dans le diff direct** [`lib/emails.ts:11`] -- fonction existante, eprouvee 12+ fonctions email. Pas un bug 3.4. A valider hors review (test XSS dedie sur templates email - dette transverse).
- **CSRF / origin check** -- Next 16 protege Server Actions par defaut via header `Origin`. Pre-existant, accepte. A reconfirmer si `serverActions.allowedOrigins` est elargi un jour.


## Deferred from: code review of 3-5-notification-automatique-waitlist-a-l-ouverture-departement (2026-05-07)

- **Race dpt re-ferme entre SELECT helper et envoi email** [`lib/notify-waitlist.ts:15-22`] — Edge case rare : entre le SELECT `nom` du dpt et l'envoi des emails de la boucle, un autre admin peut re-fermer le dpt. Le destinataire reçoit alors un email d'ouverture pour un dpt qui n'est plus ouvert au clic. Mitigation Epic 4 : re-check `ouvert=true` juste avant chaque envoi (1 SELECT supplementaire par ligne, surcout acceptable).
- **CHECK BDD mutex `target_id` / `target_id_text`** [`supabase/migrations/20260506130000_admin_actions_log_target_id_text.sql:21`] — Mutex purement applicatif. Un futur code path peut INSERT avec les deux NULL ou les deux NOT NULL sans rejet Postgres. Mitigation Epic 4 : `ALTER TABLE ... ADD CONSTRAINT target_id_xor CHECK ((target_id IS NULL) <> (target_id_text IS NULL)) NOT VALID`, puis VALIDATE apres backfill des lignes historiques.
- **Tests manuels AC16 (a)-(i) non executes en livraison story 3.5** [story file lignes 113-122] — Reportes au reviewer humain. A executer **avant** le 1er toggle reel d'un dpt hors-Bretagne en prod (waitlist actuellement vide en BDD, donc pas de regression silencieuse possible jusque la). Itineraire : (a) insert SQL test, (b) toggle UI, (c) re-toggle close/open idempotence, (d) double-click no-op, (e) toggle region, (f) curl cron retry, (g) Resend down (staging), (h) verif `target_id_text` admin_actions_log, (i) purge.


## Deferred from: code review of 3-6-audit-soft-paywall-et-corrections-d-alignement-code-prd (2026-05-07)

- **Erreurs server `getOrCreateConversation*` revelent le role cible** [`app/actions/messages.ts:64,127`] — Messages d'erreur differencies (`'... contacter une accompagnante.'` vs `'... contacter un beneficiaire.'`) permettent a un attaquant connecte d'inferer le role du compte cible (oracle). Mitigation Epic 4 : message generique unique `'Abonnement requis pour contacter.'` ou enrichir avec un identifiant non-sensible. Faible severite, surface limitee (pre-requis : compte connecte non-abonne tente getOrCreate sur un profil arbitraire).
- **`hasActiveSubscription` failure silencieuse → faux paywall** [`lib/subscription-helpers.ts:39-57`] — `.single()` echoue sur 0 row ET sur 2+ rows, et la branche `error` est destructuree (`{ data }` seul) donc une erreur transitoire (timeout, RLS) renvoie `false` -> utilisateur abonne paywalle. Story 3.6 multiplie les call sites (4 -> 7) donc le risque d'incident augmente. Mitigation Epic 4 : `.maybeSingle()` + log explicite des erreurs Supabase. Pre-existant a 3.6, hors scope D7.
- **`getOrCreateConversation*` : `existing` null sur erreur Supabase silencieuse** [`app/actions/messages.ts:51-67,107-130`] — Meme pattern que ci-dessus : `select existing.single()` destructure `data` sans capturer `error`, donc `data: null` couvre 0 row ET erreur DB. Si row existe mais erreur transitoire, idempotence cassee -> tentative INSERT (peut creer un doublon si pas de unique constraint sur `(accompagnante_id, accompagne_id)`). Mitigation Epic 4 : `.maybeSingle()` + check unique constraint en BDD.
- **Toggle `updateAnnonce*Status(id, 'publiee')` sur annonce deja publiee bumpe `published_at`** [`app/actions/annonces.ts:166-180,463-477`] — User connecte abonne peut spammer le toggle `'publiee'` pour remettre son annonce en haut du tri (effet bump). Pre-existant a 3.6 (la story ajoute uniquement le check abonnement). Mitigation Epic 4 : `if (current.status === 'publiee' && status === 'publiee') return { success: true }` sans update, ou cooldown applicatif.
- **Toggle status valeur out-of-band TS bypass** [`app/actions/annonces.ts:148-181,436-470`] — Si client custom HTTP appelle `updateAnnonce*Status(id, 'autre' as any)`, le TS guard est contourne et la valeur arbitraire est envoyee a Supabase. Si schema BDD a un CHECK constraint sur `status`, echec en DB ; sinon, donnee corrompue. Pre-existant a 3.6. Mitigation Epic 4 : whitelist explicite serveur `if (!['publiee', 'archivee'].includes(status)) return { error: 'Statut invalide.' }`.

## Deferred from: code review of 3-7-audit-cookies-scripts-tiers (2026-05-07)

- **Citation nominative "Google Analytics, Meta Pixel, Vercel Analytics, etc." dans la politique** [`app/politique-de-confidentialite/page.tsx:109-111`] — Affirmation publique citant des libs précises. Si une de ces libs est ajoutée par mégarde, l'affirmation devient fausse. Reformulation générique ("aucun outil d'analytics ou de pixel publicitaire tiers") plus robuste, mais pédagogiquement moins claire pour l'utilisateur. Reporté Epic 4 robustesse rédactionnelle.
- **Pas de mécanisme automatisé d'enforcement de la règle DECISIONS.md (audit cookies à chaque PR)** — Pas de hook CI ni de test e2e Playwright cookies. La règle DECISIONS.md ("tout nouveau cookie ou script tiers doit être audité avant merge") repose entièrement sur la garde humaine. Reporté Epic 4 hardening (déjà acté dans le rapport audit Section 6).
- **Conclusion conformité publiée avant re-audit DevTools live** — DECISIONS.md conclut formellement à la conformité tout en reconnaissant qu'un re-audit DevTools live est obligatoire avant ouverture département Bretagne. Dépendance déjà inscrite à la Définition de Done Epic 3 (re-audit obligatoire avant ouverture). Reporté Epic 4 / pré-go-live.
- **Politique de confidentialité ne qualifie pas formellement les "sous-traitants" art. 28 RGPD** [`app/politique-de-confidentialite/page.tsx:117-121`] — Vercel/Supabase/Stripe traités comme "prestataires" sans qualification art. 28 explicite. DECISIONS.md cite "sous-traitant art. 28" pour Stripe, mais ni `politique-de-confidentialite/page.tsx` ni `mentions-legales/page.tsx` ne reprennent cette qualification. Lié aux placeholders mentions-légales déjà reportés Epic 4.
- **Pas de circuit de revalidation continue de la politique** — La politique cite Google Analytics, Meta Pixel, Vercel Analytics nommément ; aucun mécanisme automatisé ne garantit que la politique reste véridique au fil des stories. Doublon avec l'item enforcement automatisé. Reporté Epic 4 hardening.

## Deferred from: code review of story-4.1 (2026-05-07)

- **`sendWaitlistOpeningNotificationEmail` early-return invalid nom sans `admin_actions_log`** [`lib/emails.ts:949-961`] — Code pré-existant story 3.x (commenté "Code review patch #13/#14"), pas introduit par 4.1. Le pattern F4 (DECISIONS.md) demande un audit trail pour toute notification perdue ; cette branche silently drops un email sans log BDD. Dette à reprendre dans une story dédiée hardening notifications.
- **`/monitoring` tunnel route : aucun `robots.txt` ni `noindex`** [`next.config.mjs:32`] — Théorique : crawlers peuvent indexer la route Sentry tunnel et générer du bruit. Hors scope story 4.1 (bonne pratique SEO/sécurité plus large à traiter avec d'autres routes techniques).
- **`SENTRY_AUTH_TOKEN` build-time only sans helper enforce le contrat** — Si un futur dev fait `process.env.SENTRY_AUTH_TOKEN` côté runtime, undefined silencieux en prod. Théorique, pas dans le diff actuel ; envisager un helper `getBuildTimeOnlyEnv()` ou un commentaire en tête de la doc env si le risque devient concret.


## Deferred from: code review of 4-2-fix-schema-log-notification-transversal (2026-05-07)

- **`fullName` variable dead** [`app/actions/contact.ts:34`] — Pre-existant story 4.2 (Dev Agent Record ligne 406 le confirme : 1 warning preexistant `fullName unused`). Variable calculee mais jamais utilisee dans le rendu HTML ni dans le payload Resend (le formulaire utilise `firstname`/`lastname` separes). Mitigation Epic 4+ : retirer la ligne ou inclure `fullName` dans le mail HTML pour un affichage plus naturel.
- **Helper `logNotification` non idempotent (pas de unique constraint)** [`lib/emails.ts:25-43`] — Pre-existant. Aucune contrainte unique sur `(user_id, type, subject, sent_at)` ou similaire. Double-click utilisateur ou retry middleware Next.js -> 2 lignes notifications_log distinctes pour le meme envoi. Le dedup `hasRecentNotification` (Stripe webhook) compense pour certains types mais pas tous. Mitigation Epic 4+ : ajouter une partial unique constraint contextuelle ou un dedup applicatif transverse story 4.3 (queue durable).
- **Pas de rate-limiting sur `sendContactMessage` -> DoS theorique BDD** [`app/actions/contact.ts`] — Pre-existant. Bot peut soumettre N fois -> N INSERTs notifications_log. Aucun captcha, aucun throttling IP. Mitigation Epic 4 hardening : reutiliser le pattern rate-limit story 4.1 (validateCode) ou ajouter un middleware route specifique au contact form.
- **Pas de validation runtime de `userId` dans `logNotification`** [`lib/emails.ts:25-43`] — Pre-existant. Caller passant `userId: ''` -> mappe a `null` (OK via `params.userId || null`), mais `userId: 'undefined'` literal -> truthy -> INSERT user_id='undefined' -> Postgres rejette (UUID parse error ou FK violation) -> exception propage. Aucune validation de format. Mitigation Epic 4+ : guard `if (params.userId && !UUID_REGEX.test(params.userId)) throw` ou normalisation amont.
- **Pas de tests unitaires sur `logNotification` ni les 21 call-sites** [`lib/emails.ts`] — Hors-scope #7 explicite story 4.2 (couvert par story 4.4 vitest+Playwright + seeds 4.7). AC2 du story 4.2 (alignement type TS / CHECK BDD) verifiable uniquement en runtime.
- **Stale comment `// skip log si pas d'userId` dans `app/actions/waitlist.ts:90` (a confirmer)** — Edge Hunter signale un commentaire obsolete. A verifier post-merge : si present, le code ne skip plus rien depuis 4.2 (suppression `canLog`). Mitigation : supprimer ou reformuler.
- **`notifications_log` retention indefinie / RGPD pour visiteurs anonymes** — Hors-scope #3 explicite story 4.2. Deja trace ligne 163 de ce document (`Stockage IP brut sans TTL ni purge -- RGPD`). Visiteur anonyme s'inscrivant waitlist + remplissant contact form -> 2+ rows avec `email` PII, `subject` parfois sensible (nom departement), aucune cle de jointure user pour cascade ON DELETE. Droit a l'oubli RGPD inapplicable. Candidat Epic 5+ avec TTL applicatif (cron purge >18 mois) ou anonymisation.
- **Aucune lint rule / hook CI pour empecher INSERT direct sur `notifications_log`** — Suggestion organisationnelle. La regle DECISIONS.md F6 (« utiliser `logNotification` exporte plutot que INSERT direct ») repose 100% sur la vigilance code-review humaine. Mitigation Epic 4+ : ajouter un grep CI dans le hook precommit ou un ESLint custom rule (`no-restricted-syntax` matchant `from('notifications_log').insert`).
- **Convention `<flow>_<event>` non respectee par `contact_form` / `welcome` / nouveaux statuts** — Pre-existant. DECISIONS F6 prescrit la convention de nommage `<flow>_<event>` mais `contact_form` est un nom de flow seul, `welcome` aussi. Les statuts `'lost'`/`'pending'` ne suivent pas non plus le pattern (mais ce sont des statuses, pas des types). Mitigation : reformuler la convention dans DECISIONS pour distinguer `type` (flow_event) et `status` (operationnel), ou refondre les noms historiques (rupture compat).


## Deferred from: code review of 4-3-email-queue-durable (2026-05-08)

- **`sendWaitlistConfirmationEmail` sans validation `isValidNomDepartement`** [`lib/emails.ts:899-947`] — Pre-existant. Asymetrie historique avec `sendWaitlistOpeningNotificationEmail` (qui valide depuis story 3.5 patch #13/14). Hors scope story 4.3 (la fonction est conservee a l'identique pour fallback synchrone AC8). Mitigation Epic 5+ : extraire la validation `isValidNomDepartement` deja dans `lib/email-templates.ts` et l'appeler en early-return dans `sendWaitlistConfirmationEmail` pour symetrie defensive (header injection theorique sur le subject).

- **Resend client recree a chaque retry du step `sendEmailViaResend`** [`lib/workflows/send-email-workflow.ts:125`] — Decision D5 documentee : ~5ms x N envois revendique negligeable. Sur Ile-de-France pire cas (200 batch x 4 attempts = 800 instances) le surcout cumule peut atteindre ~4s CPU gaspille. Mitigation Epic 5+ : module-level singleton `const resend = new Resend(...)` (Resend SDK est stateless HTTP, fork-safe).

- **`RESEND_FROM_EMAIL` fallback `onboarding@resend.dev` accepte en prod** [`lib/workflows/send-email-workflow.ts:36` + `lib/emails.ts:10`] — Pre-existant. `onboarding@resend.dev` est le from default Resend dev sandbox (limite 100 emails/jour, taux de bounce eleve, accepte uniquement par l'owner du compte Resend en prod). Si la variable manque silencieusement en prod Vercel, les emails partent depuis ce domaine -> bounces massifs. Candidat story 4.8 (REQUIRED vs OPTIONAL_ON_PREVIEW) pour promouvoir `RESEND_FROM_EMAIL` en REQUIRED prod.

- **Rate-limit ne couvre pas l'enqueue downstream (DoS via IP rotation -> saturation queue)** [`app/actions/waitlist.ts:54-64`] — Pre-existant. Le rate-limit 5 req/10min/IP s'applique avant l'INSERT et l'enqueue. Bot avec rotation IP peut contourner et faire saturer le quota Vercel Workflow. Candidat story 4.5 (hardening IP spoofing) qui peut traiter le rate-limit downstream queue + protection serveur applicative globale.

- **Idempotency `runId-stepId` n'evite pas double-envoi entre runs** [`lib/workflows/send-email-workflow.ts:127-138`] — Tradeoff documente decisions D2/D10. Le `stepId` est stable across retries du MEME step mais un nouveau run (ex: cron retry sur ligne dont `notified_at IS NULL` + 1er run avait envoye sans confirmer notified_at) genere un nouveau `runId/stepId` -> Resend ne deduplique plus. Le compare-and-swap `notified_at` reste la vraie protection cote admin trigger. Candidat amelioration Epic 5+ via partial unique index BDD `notifications_log` sur `(email, type, hash(subject), date_trunc('hour', sent_at))` ou idempotency_key applicatif deterministe `SHA-256(template+to+variables+date)`.


## Deferred from: code review of stories 4.4 + 4.7 (2026-05-09)

- **Vercel build cassable sans `SKIP_E2E_TESTS=true`** [`vercel.json:2`] — Action manuelle Sylvain deja prise (env var Production set 2026-05-09). Pas de guard automatique : si la var est retiree ou si une nouvelle env Vercel est creee sans, tous les deploys cassent. Backstop possible : etendre `scripts/check-required-env.mjs` pour throw si Vercel build sans `SKIP_E2E_TESTS=true`. Story 4.4.b candidate.
- **`supabase/setup-cli@v1` + `version: latest` mutable tags** [`.github/workflows/integration-tests.yml:28-30`] — Risque supply chain. Pinner sur SHA commit + version specifique. Durcissement post-stabilisation 7j (Epic 5+ ou story 4.4.b).
- **Workflow GHA `Stop Supabase --no-backup` ne capture pas les logs en cas dechec** [`.github/workflows/integration-tests.yml:56-58`] — Debugging flaky difficile. Ajouter `actions/upload-artifact` pour `supabase logs` en cas dechec. A activer si > 1 flaky observe pendant 7j stabilisation.
- **Tracker module-level non isole entre fichiers Vitest** [`tests/integration/_lib/fixtures.ts:10`] — `fileParallelism: false` + `pool: forks` rend chaque fichier isole en pratique, mais le pattern reste fragile. Pas de balayage email LIKE final implemente (seulement documente README). Implementer balayage defensif ou AsyncLocalStorage si flaky.
- **Multiples chemins non gardes seed/cleanup (~30 findings Edge Case Hunter)** — BATCH_LIMIT chunking sur `delete().in()`, BOM/encoding seed files, ECONNREFUSED race Postgres 54322, FK cascade undefined sur `auth.users -> public.users`, retry idempotency seed file partial fail, `tracker[]` auto-vidage `length=0`. Aucun na casse les 2 runs CI verts (#25504890455, #25505618623). Durcissement par grappes post-stabilisation, voir Edge Case Hunter raw findings (transcript review 2026-05-09).
- **Sprint-status comment ligne 132 stale (sequencage 4.7 avant 4.2)** [`_bmad-output/implementation-artifacts/sprint-status.yaml:132`] — Cleanup mineur, pas bloquant merge. Mettre a jour lors de la prochaine modification du fichier.
- **Pas de transaction wrapping multi-fichiers dans seed script** [`scripts/seed-test-supabase.mjs:103-110`] — Failure partielle entre 02_parrainages et 03_waitlist laisse rows orphelines. Limite acceptee tant que tests verts. Durcissement Epic 5+ via Postgres `BEGIN; ... COMMIT;` global.
- **Pas de check `data.user.id === user.id` apres `auth.admin.createUser`** [`scripts/seed-test-supabase.mjs:96-103`] — Versions anciennes supabase-js (pre-2.41) peuvent ignorer le param `id`. Faux positif probable (`@supabase/supabase-js@^2.95.3` actuel supporte). Durcissement defensif Epic 5+ : assertion runtime + warn si mismatch.
- **Pas de retry/wait sur `pgClient.connect` (port 54322)** [`scripts/seed-test-supabase.mjs:152`] — Race possible avec `supabase start` boot. CI runs verts au 2/2. Durcissement si flaky.

## Deferred from: code review of story-4.8 (2026-05-07)

- **`VERCEL_ENV='development'` (vercel dev) tombe dans branche dev local sans branche dediee** [`scripts/check-required-env.mjs:40-65`] — Pre-existant story 3.8, conforme D4 spec « dev local inchange ». Si Vercel introduit comportement specifique a `development` (vs undefined), revisiter.
- **`SENTRY_AUTH_TOKEN` documente dans `.env.local.example` mais absent du check `check-required-env.mjs`** [`scripts/check-required-env.mjs` vs `.env.local.example:41-45`] — Adresse explicitement par la note ajoutee `NEXT_STEPS.md:298` « non verifiee par check:env ». Choix conscient documente. Effet : sans le token en prod, sourcemaps non uploadees silencieusement, stack traces Sentry restent minifiees. Audit dependance vars d'env = story Epic 5+.
- **`NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` utilises avec assertion `!` dans `lib/supabase/{client,server,middleware}.ts` mais absents du check** [`scripts/check-required-env.mjs`] — Pre-existant 3.8. Hors scope explicite story 4.8 (« liste figee a 14 vars » D7 spec). Deploy avec ces vars manquantes : `check:env` passe, runtime crash au premier hit. Audit dependance vars Epic 5+.
- **`isMissing` accepte placeholders litteraux** [`scripts/check-required-env.mjs:36`] — Hors scope explicite story 4.8 ligne 38 « Validation de format des valeurs : trop de surface, hors scope ». `your_random_secret`, `your_supabase_anon_key` etc. passent le check. Foot-gun classique copy-paste `.env.local.example`. Durcir avec regex shape (`STRIPE_SECRET_KEY` doit commencer par `sk_`, `ENCRYPTION_KEY` doit etre 64 char hex, etc.) si Epic 5+.
- **`vercel.json` masque exit code des E2E tests via `(test "$SKIP_E2E_TESTS" = "true" || npm run test:integration)`** [`vercel.json`] — Orthogonal 4.8 (heritage story 4.4 stabilisation 7j flag). Si `SKIP_E2E_TESTS=true` leak en prod env (regression Vercel scope), prod deploys silencieux sans tests integration. Securisation : ajouter assertion `SKIP_E2E_TESTS doit etre absent en VERCEL_ENV=production` dans `check-required-env.mjs` ou separer le buildCommand prod/preview.
- **CI GHA `integration-tests.yml` ne lance pas `check:env`** [`.github/workflows/integration-tests.yml`] — Premier signal vars manquantes = build Vercel preview, pas PR check GHA. Acceptable design car redondant. Durcissement si on veut des PR checks plus precoces.
- **Description `RATE_LIMIT_HASH_SALT` perd la mention « fallback SHA-256 non-sale » dans le code** [`scripts/check-required-env.mjs:30`] — Ancienne version 4.1 patch review avait commentaire `// productionOnly: sans sel en preview/dev, le helper degrade en SHA-256 non-sale`. Refactor 4.8 a deplace le contexte vers `NEXT_STEPS.md` section OPTIONAL_ON_PREVIEW. Pas de regression, juste relocalisation. Operateur fixant la var en error build a moins de contexte inline mais la doc est trouvable.
- **Emoji `🔐` heritage story 3.8 dans heading `NEXT_STEPS.md:273`** [`NEXT_STEPS.md:273`] — Pre-existant a 4.8 (introduit 3.8). Regle CLAUDE.md « Pas d'emojis dans le code ni les interfaces » — applicabilite docs Markdown ambigue. Cleanup follow-up doc-pass.
- **Risque rollout : si scope preview Vercel n'a pas les 9 REQUIRED set, mass-red builds preview au merge** — Operationnel, pas un bug code. Regression cible AC3 (durcir preview pour eviter crash runtime sur var REQUIRED absente). AC14 documente deja la validation post-merge requise. Validation `vercel env ls --environment preview` ou dashboard Vercel recommandee avant merge si doute.

## Deferred from: code review of story-4.5 (2026-05-09)

- **Tests IPv6 + header injection `\r\n` non couverts** [`tests/unit/get-client-ip.test.ts`] — Hors scope explicite §Hors-scope §2 spec story 4.5 (no normalization, BDD TEXT et rate-limit key opaques). Pas de risque d'exploit BDD via params Supabase parametrises. Si une story future Epic 5+ ajoute geolocation/analytics IP qui parse semantiquement la valeur (parser IPv6, lookup geoip), ajouter alors U7/U8 IPv6 + injection `\r\n`.
- **AC16 verifications post-deploy preview Vercel** [`_bmad-output/implementation-artifacts/4-5-hardening-ip-spoofing.md:AC16`] — (f) test regression rate-limit `validateCode` 6 tentatives, (g) waitlist 6 inscriptions <10 min, (h) audit BDD `parrainages.ip_inscription` via Supabase MCP, (i) verification ad-hoc `x-real-ip` non-null en preview. Defere par design (executable apres ouverture PR sur deploy preview Vercel, pas blocant code review). A executer dans la fenetre de validation PR avant merge main.

## Deferred from: code review of story-4.6 (2026-05-09)

- **`(profile.specialites as string[]).map(...)` sans fallback `|| []`** [`app/admin/validation/[id]/page.tsx:40`] — Pre-existant (ligne presente avant le diff story 4.6). Crash 500 si `specialites = null` (schema BDD `string[] | null`). Comparer ligne 36 `(profile.diplomes as string[] || []).map(...)` qui a le fallback. Candidat story 4.6.b (factories Supabase typees + audit Json/array casts hors-admin) ou story d'hardening admin UX.
- **`new Date(u?.created_at).toLocaleDateString(...)` rend "Invalid Date" si `u` null** [`app/admin/validation/[id]/page.tsx:126`] — Pre-existant. Si la jointure user retourne null (FK orpheline, RLS, race entre suppression user et validation), affichage "Invalid Date" sans crash mais visuel casse. Suggestion : guard `u?.created_at ? new Date(u.created_at).toLocaleDateString('fr-FR') : '-'`.
- **N+1 queries unread counts admin messages** [`app/admin/messages/page.tsx:30-39`] — Pre-existant (boucle non touchee par le diff story 4.6, revelee par le typage propre post-resorption `as any`). Pour chaque conversation, un appel separe a `messages` pour le count. 100 conversations admin -> 101 round-trips Supabase. Latence elevee >50 conversations, timeout possible Vercel. Suggestion : aggregation cote BDD (RPC ou GROUP BY).
- **AC17 audit Sentry quotidien D+0..D+7 + audit MCP `list_tables` D+7** [`_bmad-output/implementation-artifacts/4-6-resorption-as-any-pages-admin.md:AC17`] — Defere par definition de l'AC (post-merge). A executer dans la fenetre de surveillance 7 jours apres merge sur main pour confirmer absence de regression Supabase typage strict (erreurs schema-drift, jointures cassees).

## Deferred from: code review of story-4.6 pass 2 (2026-05-09)

- **`app/admin/page.tsx:134` `(profile.diplomes as string[] || [])` reste cast applicatif** [`app/admin/page.tsx:134`] — Hors scope strict story 4.6 (`\bas any\b` only). Pattern similaire a `validation/[id]/page.tsx:40` `specialites as string[]` mais avec fallback `|| []` correct. Pas de bug, juste pattern non-strict perpetue. Candidat story type-safety BDD ulterieure (resolu naturellement quand factories Supabase generic-typees `<Database>`).
- **`annonces/page.tsx:80,116` `type as 'accompagnante' | 'accompagne'` non valide URL** [`app/admin/annonces/page.tsx:80,116`] — `params.type` lu brut depuis URL, defaultee `'accompagnante'`, puis cast force. Si URL `?type=foo` ou `?type=Accompagnante` (capital), les deux branches choisissent count-only (`head: true`), `rawAnnonces = []` -> page rend "0 annonces" silencieusement. Pre-existant URL handling. Candidat hardening admin UX : valider via `const TYPE_VALUES = ['accompagnante','accompagne'] as const` + fallback explicite.
- **`check-as-any-admin.mjs` import `typescript` fragile si move devDeps + Vercel `--omit=dev`** [`scripts/check-as-any-admin.mjs:24`] — Le script s'execute *avant* `next build` dans `vercel.json:buildCommand`. Si un futur cleanup deplace `typescript` de `dependencies` vers `devDependencies`, et si Vercel run avec `--omit=dev`, le build casse avec module-not-found avant `next build`. Aujourd'hui `typescript` est en `dependencies` (`package.json:62`), scenario hypothetique. Ajouter regle projet "typescript reste en deps tant que check-as-any-admin.mjs l'importe" ou refactor en spawn `npx tsc --check ...` (subprocess autonome) Epic 5+.
- **Spec narratif `4-6-resorption-as-any-pages-admin.md:17` annonce "6 enums" mais types/supabase.ts contient 7 enums** — Drift documentaire spec, pas code. `Database.public.Enums` contient `annonce_status`, `creneau_horaire`, `jour_semaine`, `niveau_dependance`, `subscription_status`, `user_role`, `validation_status` = 7 enums. Spec liste les 7 noms mais decompte "6 enums". A corriger en story closure note ou regen spec post-merge si la divergence devient bruit.

## Deferred from: code review of stories 7.A.1 + 7.A.2 (2026-05-14)

- **Cron `confirm-parrainages` capture le throw 7.A.1 et l'agrege en compteur `errors` indistinct** [`app/api/cron/confirm-parrainages/route.ts:48`] — La story 7.A.1 introduit `throw` fail-loud dans `hasActiveSubscription` mais le cron capture l'exception dans un `try/catch` de boucle qui increment `errors` sans distinguer erreur-metier vs incident-BDD transitoire. Resultat : on remplace "faux paywall utilisateur visible" par "metrique cron pollute invisible aux ops". Candidat helper `withSubscriptionCheck` qui differencie les 2 classes.
- **`getOrCreateConversationAsAccompagnante` ne cree pas le profil s'il manque** [`app/actions/messages.ts:176-191`] — Asymetrie avec `getOrCreateConversation` qui auto-cree `accompagnes_profiles`. Voulu historiquement (validation_status accompagnant requis) mais bloque silencieusement les accompagnants orphelins. Candidat clarification produit + redirection `/accompagnant/onboarding`.
- **Magic strings Sentry tags repetes (`subscription_check`, `messaging`, `profile-lookup-error`) — pas d'enum** [`lib/subscription-helpers.ts`, `app/actions/messages.ts`] — Un typo casse silencieusement le filtrage Sentry. Refactor enum/const candidat story hygiene.
- **`Sentry.captureException` duplique 6+ fois sans helper `captureProfileLookupError(err, scope)`** [`app/actions/messages.ts`] — Pattern repetitif identique. Helper trivial reduirait le bruit visuel et faciliterait l'ajout de `extra:` standardise.
- **Message UX "Impossible de verifier votre profil." trop generique (5 contextes, pas d'eventId)** [`app/actions/messages.ts`] — Support ticket ne peut pas etre lie a l'event Sentry. Candidat ajout `eventId` retourne dans `{ error, eventId }` + UI affiche "Ref: XXX" pour le support.
- **Pas de `user_id` (ni hash) en tag Sentry sur `hasActiveSubscription`** [`lib/subscription-helpers.ts:55`] — Debug d'incidents partiels (RLS denied sur sous-ensemble) difficile sans correlation user. Trade-off PII : `extra: { userIdHash }` acceptable.
- **`cleanEnv` test n'herite que PATH/HOME — fragile Windows / CI corporate** [`tests/unit/check-required-env.test.ts:52-60`] — Manque `SystemRoot`/`APPDATA` (Windows) et `NODE_EXTRA_CA_CERTS` (CA corporate). OK actuellement mono-OS macOS/Linux, candidat hardening si matrix Windows ajoutee.
- **Helpers `minLength`/`matches` retournent `true | string` (anti-pattern type-faillible)** [`scripts/check-required-env.mjs:32-45`] — Refactor `{ ok, message }` typed. Low priority internal helpers.
- **`vi.mocked(createClient).mockResolvedValue(fakeClient as never)` masque type incorrect** [`tests/integration/paywall/erreur-supabase-transitoire.test.ts:44`] — Typage strict Supabase Database trop verbeux pour le scope test, mais perd la verification de la signature.
- **Mock chaine `.from().select().eq().in().maybeSingle()` replique signature interne SUT — couplage trompeur** [`tests/integration/paywall/erreur-supabase-transitoire.test.ts`] — Si l'ordre des chainages SDK change (ajout `.order()`, swap eq/in), mock retourne `undefined` et test casse pour rien. Candidat refactor wrapper testable.
- **`EMAIL_DISPLAY` regex accepte tab/control chars/quotes non echappees** [`scripts/check-required-env.mjs:28`] — Validation shallow, Resend SDK fait la vraie validation cote API. Acceptable mais documenter le caractere indicatif.
- **Asymetrie preview "absent OK / present invalide refuse" cree faux signal "preview vert != prod-ready"** [`scripts/check-required-env.mjs:163-178`] — Un deploy preview qui passe le check ne garantit PAS qu'un deploy prod equivalent passerait. Documenter dans DECISIONS.md "post-promote rebuild requis".
- **`OPTOUT_TOKEN_SECRET` REQUIRED tout court sans `requiredOnPreview: false`** [`scripts/check-required-env.mjs:74-75`] — Friction preview branches sans la var. Verifier si emails de rappel sont actifs en preview ; si non, basculer en OPTIONAL_ON_PREVIEW.
- **`EMAIL_BASIC` accepte TLD 1 char (`a@b.c`)** [`scripts/check-required-env.mjs:26`] — Pas de detection typo court. Bonus hardening hors scope strict.
